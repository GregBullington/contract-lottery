{
  "name": "contract-lottery",
  "scripts": {
    "compile": "node compile.js",
    "test": "node compile.js && mocha --timeout 30000 test/SimpleStorage.test.js",
    "test:debug": "DEBUG=true npm run test",
    "deploy": "node deploy.js"
  },
  "dependencies": {
    "solc": "^0.8.17"
  },
  "devDependencies": {
    "ethers": "^5.7.0",
    "fs-extra": "^10.1.0",
    "ganache-core": "^2.13.2",
    "mocha": "^10.0.0"
  }
}
